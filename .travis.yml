language: python
python:
  - "3.5"

sudo: required

services:
  - docker

install:
  - pip install -r dev_requirements.txt
  - pip install .
  - ./generate_certs.sh

script:
  - sudo service docker stop
  - sudo /home/travis/virtualenv/python3.5/bin/nose2
